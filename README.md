# LaTeX Template with Visual Studio Code

This is an empty template to write LaTeX documents
and build them with Visual Studio task runner.

# Prerequisite

Install TeXLive or MacTeX.

# Build

Simple. Run:
```bash
$ latexmk
```